# apiVersion: networking.k8s.io/v1
# kind: Ingress
# metadata:
#   name: {{ .Values.Ingress.name }}
#   namespace: {{ .Values.sharedNamespace }}
#   annotations:
#     # kubernetes.io/ingress.class: traefik # Deprecated
#     traefik.ingress.kubernetes.io/router.middlewares: {{ .Values.sharedNamespace }}-{{ .Values.backend.middleware.name }}@kubernetescrd
# spec:
#   ingressClassName: "traefik"
#   rules:
#     - http:
#         paths:
#           - path: /api
#             pathType: Prefix
#             backend:
#               service:
#                 name: {{ .Values.backend.service.name }}
#                 port:
#                   number: {{ .Values.backend.service.port }}
