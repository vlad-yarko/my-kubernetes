apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-{{ .Values.secretName }}
  namespace: {{ .Values.sharedNamespace }}
type: Opaque
stringData:
  DATABASE_URL: "postgres://postgres:1234@{{ .Release.Name }}-postgresql.{{ .Values.sharedNamespace }}.svc.cluster.local:5432/postgres" # BAAAAAD
