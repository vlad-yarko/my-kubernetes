helmChart:
  repo: oci://kubernetes-yep-registry.duckdns.org/my-kubernetes/prometheus-chart-k
  chartVersion: 1.0.7
  releaseName: my-kubernetes-prometheus-{{ args.environment }}
  # namespace: {{ namespaces.application.name }}
  # namespace: default
  namespace: my-kubernetes-monitoring
  output: helm-rendered.yaml
  # skipPrePull: true
  # valuesFiles: 
  #   - helm-values-{{ args.environment }}.yaml
