helmChart:
  repo: oci://kubernetes-yep-registry.duckdns.org/my-kubernetes/promtail-chart-k
  chartVersion: 1.0.1
  releaseName: my-kubernetes-promtail-{{ args.environment }}
  # namespace: {{ namespaces.application.name }}
  # namespace: default
  namespace: my-kubernetes-monitoring
  output: helm-rendered.yaml
  # skipPrePull: true
  # valuesFiles: 
  #   - helm-values-{{ args.environment }}.yaml
